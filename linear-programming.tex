\setlength{\tabcolsep}{6pt}
\begin{tabu} to \linewidth {X[-2.5, c, m] | X[l,m]}
  \bf standard \newline (inequality) \newline form &
  minimize $\displaystyle \sum_{i=1}^n c_ix_i$ under the constraints \\ \cline{1-1}
  \onecolx{1.33}{%
  $\displaystyle \sum_{i=1}^n a_{ji}x_i = b_j$ for $j = 1, \ldots, m$, and
  $x_i \geq 0$ for $i = 1, \ldots, n$}\\ \hline
  \bf canonical \newline form & minimize $\displaystyle \sum_{i=1}^n c_ix_i$
  under the constraints \\ \cline{1-1}
  \onecolx{1.33}{%
  $\displaystyle \sum_{i=1}^n a_{ji}x_i \geq b_j$ for $j = 1, \ldots, m$, and
  $x_i \geq 0$ for $i = 1, \ldots, n$ \vspace{1mm} \newline
  to move to standard form, add a new variable $s_j$ ({\bf surplus} variable)
  for every inequality constraint and instead require: \newline
  \phantom{------------}
  $\displaystyle \sum_{i=1}^n a_{ji}x_i - s_j = b_j$, $s_j \geq 0$
  for $j = 1, \ldots, m$} \\ \hline
  \bf standard \newline inequality \newline form &
    minimize $\displaystyle \sum_{i=1}^n c_ix_i$ under the
  constraints \\ \cline{1-1}
  \onecolx{1.33}{%
  $\displaystyle \sum_{i=1}^n a_{ji}x_i \leq b_j$ for $j = 1, \ldots, m$, and
  $x_i \geq 0$ for $i = 1, \ldots, n$ \newline
  to move to standard form, add a new variable $s_j$ ({\bf slack} variable) for
  every inequality constraint and require: \newline
  \phantom{------------}
  $\displaystyle \sum_{i=1}^n a_{ji}x_i + s_j = b_j$, $s_j \geq 0$
  for $j = 1, \ldots, m$}\\ \hline
  \bf general \newline form &
  minimize $\displaystyle \sum_{i=1}^n c_ix_i$ for an input instance \\ \cline{1-1}
  \onecolx{1.33}{%
  $A = [a_{ji}]_{j=1,\ldots,m,i=1,\ldots,n}, b = (b_1,.. , b_m)^{\mathbf{T}},
  c  = (c_1, .., c_m)^{\mathbf{T}}$, 
  \phantom{---------------------}
  $M \subseteq \{1, \ldots, m\}, Q \subseteq \{1, \ldots, n\}$
  \newline
  under the constraints $\displaystyle\sum_{i=1}^n a_{ji}x_i = b_j$
  for $j \in M$ \newline
  \phantom{------------------}
  $\displaystyle \sum_{i=1}^n a_{ri}x_i \geq b_r$
  for $r \in \{1, \ldots, m\} - M$ \newline
  \phantom{---------------------------}
  $x_i \geq 0$ for $i \in Q$}\\ \hline
  \bf hyperplane & An affine subspace of $\R^n$ of dimension
  $n - 1$, $n \in \N^+$. \\ \cline{1-1}
  \onecolx{1.33}{%
  alternatively, a hyperplane of $\R^n$ is a set of all $X = (x_1,
  \ldots, x_n) \in \R^n$ such that $a_1x_1 + \ldots + a_nx_n = b$ for some $a_1,
  \ldots, a_n, b$ where not all $a$'s are equal to zero.}\\ \hline
  \bf halfspace & The sets $HS \geq (a_1, \ldots, a_n b)$ $\{X = (x_1, \ldots,
  x_n) \in \R^n \mid \sum_{i=1}^na_ix_i \geq b\}$ and the set defined as above,
  but with $\geq$ replaced with $\leq$. \\ \hline
  \onecolx{1.33}{%
  The intersection of a finite number of halfspaces of $\R^n$ is a {\bf convex
    polytope} of $\R^n$, $n \in \N^+$. For given constraints
  $\sum_{j=1}^na_{ji}x_i \leq b_j$ for $j = 1, \ldots, m$ and $x_i \geq$ for
  $i = 1, \ldots, n$ $\mathbf{Polytope(AX \leq b, X \geq 0_{n\times1})} = \{X
    \in (R^{\geq 0}) \mid AX \leq b\}$.} \\ \hline
  \onecolx{1.33}{%
  Let $d, n \in \N^+$, and $A$ be a convex polytope of dimension $d$ in $\R^n$.
  Let $H$ be a hyperplane of $\R^n$ and let $HS$ be a halfspace defined by $H$.
  If $A \cap HS \subseteq H$ (i.e. $A$ and $HS$ just ``touch in their
  exteriors''), then $A \cap HS$ is called a {\bf face of \textit{A}}, and $H$
  is called the {\bf supporting hyperplane defining $\mathbf{A \cap HS}$}.}
  \\ \hline
  \onecolx{1.33}{%
  We distinguish the following three kinds of faces:
  \begin{enumeratex}
  \item A {\bf facet of \textit{A}} is a face of dimension $n - 1$
  \item An {\bf edge of \textit{A}} is a face of dimension $1$ (i.e. a line
  segment)
  \item A {\bf vertex of \textit{A}} is a face of dimension $0$ (i.e. a point)
  \end{enumeratex}} \\ \hline
\end{tabu}
